This folder will contain a React Native (Expo) mobile app that consumes the FastAPI backend at mobile_backend/main.py.

To create the app locally:
1. install Node and Expo CLI
2. run `npx create-expo-app mobile_app` or use the provided scaffold

The initial integration will focus on calling the /analyze endpoint and displaying the returned JSON.